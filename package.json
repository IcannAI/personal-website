{
  "name": "personal-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "prepare-out": "npm run build && touch out/.nojekyll",
    "deploy": "npm run build && touch out/.nojekyll && gh-pages -d out --dotfiles"
  },
  "dependencies": {
    "framer-motion": "^10.12.16",
    "next": "13.5.6",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "gh-pages": "^6.1.1",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.4"
  }
}